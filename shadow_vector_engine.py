# Modul za ulazne vektore i kombinatoriku
def get_vectors():
    return [
        "?q={payload}",
        "?search={payload}",
        "?input={payload}",
        "?query={payload}",
        "?s={payload}"
    ]
